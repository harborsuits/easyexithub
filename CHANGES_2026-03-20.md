# Easy Exit Dashboard Changes — 2026-03-20

## ✅ Completed

### 1. PipelinePage.tsx — Fixed to Use Canonical `pipeline_stage`

**Changes:**
- ✅ Removed `deal_stages` table query entirely
- ✅ Now queries `leads.pipeline_stage` column directly
- ✅ Uses `PIPELINE_STAGES` array from `src/types/index.ts` for column definitions
- ✅ Groups leads by their `pipeline_stage` value
- ✅ Drag-drop now updates `leads.pipeline_stage` (not `deal_stage_id`)
- ✅ Added Supabase realtime subscription on `leads.pipeline_stage` changes
- ✅ Calendar tab remains unchanged (as requested)
- ✅ Preserved existing card design (temperature, comm summary, next action, etc.)

**Pipeline stages:**
```
new, attempting_contact, follow_up_scheduled, callback_pending, 
needs_human_followup, offer_prep, negotiating, under_contract, 
closed_won, closed_lost
```

---

### 2. DialQueuePage.tsx — Enhanced as Operator Console

**A. Top Bucket Tabs (replaced filter buttons):**
- ✅ **Due Now** — pending follow_ups where scheduled_for <= now AND inside call window AND today
- ✅ **Upcoming Today** — pending, scheduled_for is today but not yet due
- ✅ **Callbacks** — follow_ups where kind = 'callback' or 'scheduled_callback'
- ✅ **Fresh Leads** — follow_ups where kind = 'initial_outreach' or 'cold_call'
- ✅ **Retries** — follow_ups where kind contains 'retry'
- ✅ **Blocked / Filtered** — leads where callable=false OR outbound_approved=false OR engagement_level in (dead, dnc)
- ✅ Count badges on each tab

**B. Switched from table to card layout:**

Each card displays:
- ✅ **Left side:** owner name (link to /leads/:id), property address, phone number (mono font)
- ✅ **Center badges:**
  - Pipeline stage badge
  - Last disposition badge
  - Engagement level chip (hot/warm/cold/dead/dnc with colors)
  - Distress chips from `property_data.distress_signals` (if array)
  - Suppression/warning chips
- ✅ **Right side metadata:**
  - Local contact time (formatted in ET)
  - Next scheduled time
  - Last touch (from communications latest created_at)
  - Queue priority number (color-coded: >=8 red, >=5 amber, else gray)
  - Source/import source (from property_data.lead_source or lead_source column)
- ✅ **Queue reason badge** prominently displayed: callback_due, retry_due, fresh_lead, manual_test_approved, etc (derived from follow_up.kind + follow_up.reason)

**C. Per-card action row:**
- ✅ Call Now (only for "Due Now" bucket)
- ✅ Open Lead (link to /leads/:id)
- ✅ Reschedule (date picker dialog)
- ✅ Skip Once (sets status='held')
- ✅ Suppress/DNC (updates engagement_level to 'dnc')
- ✅ Mark for Human Follow-up (updates pipeline_stage to 'needs_human_followup')

**D. "Explain Why Queued" expandable panel:**
- ✅ Collapsible section per card using shadcn Collapsible component
- ✅ Shows:
  - follow_up.kind
  - follow_up.scheduled_for
  - follow_up.reason
  - follow_up.source (if exists)
  - lead.callable status
  - lead.outbound_approved status
  - lead.engagement_level
  - Cold attempts count (lead.cold_attempts) + max
  - follow_up.priority
  - follow_up.status

**E. Badges system:**
- ✅ 🔔 callback pending (kind contains callback)
- ✅ 👤 human follow-up (pipeline_stage = needs_human_followup)
- ✅ 🚫 DNC / blocked (engagement_level = dnc or dead)
- ✅ 🕐 timezone (show ET formatted time)
- ✅ 🔥/🌡️/❄️/💀 engagement level
- ✅ Priority number badge (color-coded: >=8 red, >=5 amber, else gray)
- ✅ Queue reason badge with custom logic

**F. Existing functionality preserved:**
- ✅ 30-second auto-refresh
- ✅ Call window logic (10:30-12:00 ET and 16:30-18:30 ET)
- ✅ Reschedule dialog
- ✅ Call trigger via edge function (`trigger-call`)
- ✅ Hold/cancel mutations

---

## Technical Notes

- Supabase client: `src/integrations/supabase/client.ts`
- UI components: shadcn at `src/components/ui/`
- Collapsible component: `src/components/ui/collapsible.tsx` (exists, used)
- TypeScript compiles cleanly ✅
- Build succeeds ✅

## Files Modified

1. `src/pages/PipelinePage.tsx` (14.5 KB)
2. `src/pages/DialQueuePage.tsx` (26.6 KB)

**No other files touched.**

---

## Deploy Triggers

- PipelinePage.tsx: deploy trigger 1773948583
- DialQueuePage.tsx: deploy trigger 1773948584
